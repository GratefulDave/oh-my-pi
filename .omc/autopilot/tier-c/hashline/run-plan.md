# Factory Run Plan: Group H Shim Removal + Version Bump

Generated: 2026-05-28 (factory-orchestrator)
Predecessor: Group H PR1 merged (PR #12, hashline rewrite + `computeFileHash` shim)
Supersedes scope in `followups.md` where it conflicts (see §2 corrections).

## 1. Goal

- **Outcome:** All fork consumers off the `computeFileHash` backward-compat shim; shim deleted; version bumped; cumulative Tier A+B+C CHANGELOG.
- **Scope:** 6 src migration PRs + 1 shim-removal/test-reauthor PR + version bump + CHANGELOG.
- **Non-goals:** No git merge upstream/main. No new upstream feature ports. natives loader hardening is OPTIONAL/out-of-band.

## 2. Current Evidence (ground-truthed against repo, not followups.md)

| Item | Status | Provenance |
| ---- | ------ | ---------- |
| `getFileSnapshotStore(session)` helper | EXISTS | `coding-agent/src/edit/file-snapshot-store.ts:19` |
| read.ts | PARTIAL — already uses shared store at :150/:1061/:1882; residual `computeFileHash` at :127 | grep |
| search.ts | PARTIAL — already uses `recordSparse` at :673; residual shim use | grep |
| ast-grep.ts | PARTIAL — already uses `recordSparse` at :270; residual shim use | grep |
| ast-edit.ts | FULL migration — still on shim | grep |
| file-mentions.ts | FULL migration — still on shim | grep |
| typescript-edit-benchmark/src/runner.ts | FULL migration — still on shim | grep |
| docs-index.generated.ts | NOT a call site — embedded doc text, `DO NOT EDIT`, regenerated by `scripts/generate-docs-index.ts` | head -5 |
| test/core/hashline.test.ts | 14 shim sites — re-author in PR7 | grep -c |
| test/edit-diff.test.ts | 2 shim sites (import + :241) — **EXTRA, missing from followups.md** — re-author in PR7 | grep |
| Versioning | 10 packages version-synced at 15.5.7 (root `omp-monorepo` is private, no version) | grep |
| Worktrees | clean — only main + unrelated extension detached HEAD | git worktree list |
| Shim def | `hashline/src/format.ts:113` (throwaway store, unresolvable tag, display-only-safe) | Read |

**Discovery-underestimation flag (recurring):** followups.md said haiku-mechanical + 6 src files only. Reality: 3 of 6 are partial (residual removal), edit shape needs judgment (sonnet not haiku), and `edit-diff.test.ts` is an extra test consumer. Pattern matches Group H/I prior underestimates — confirm per-file before bulk execution.

## 3. Lane Sequence

| Order | Lane | Owner | Input | Output | Gate |
| ----- | ---- | ----- | ----- | ---- | ---- |
| 1 | Implementation: 6 migration PRs (parallel worktrees) | executor (sonnet) | this plan | 6 branches `parity/hashline-migration-*` | per-PR verify |
| 2 | Verifier (per PR) | verifier (sonnet) | each branch | touched-file test green, `tsc` no new errors, no new biome | blocks PR merge |
| 3 | PR/CI | pr-steward | verified branches | 6 PRs vs GratefulDave/oh-my-pi | CI green |
| 4 | Implementation: shim-removal PR7 | executor (sonnet) | PRs 1-6 merged | branch `parity/hashline-shim-removal` | blocks on 1-6 |
| 5 | Verifier PR7 | verifier (opus) | PR7 branch | `grep -rn computeFileHash packages/*/src` = 0, full coding-agent+hashline test green | blocks merge |
| 6 | Version bump + CHANGELOG | executor (sonnet) | PR7 merged | 10 package.json bumped + CHANGELOG | **semver gate (user)** |
| 7 | (OPTIONAL) security: natives loader hardening | security-reviewer | — | separate PR | deferred |

## 4. Instance Boundary

- **In-context skills:** factory-orchestrator (this), autopilot execution loop.
- **Subagents:** 6 executor (sonnet) for migrations; verifier per PR; 1 executor for shim removal; 1 opus verifier for final gate.
- **Separate OMP instance:** NOT justified — no live watchers/credentials. Single autopilot context with worktree-per-PR parallelism.

## 5. Handoffs

- **Implementation handoff (per migration):** ONE file. Remove `computeFileHash` import + call. For display headers, source the tag from `getFileSnapshotStore(session).recordContiguous(absolutePath, 1, lines, { fullText })` (or `recordSparse` if sparse). Reuse the file's existing store call if one already records the same path. Forbidden: touching other files, inventing throwaway `new InMemorySnapshotStore()` when a session store is reachable. Escalate if an extra call site or cross-file coupling appears.
- **Verifier handoff:** run the touched module's test file + `tsc -p packages/coding-agent` (or typescript-edit-benchmark); compare against same-worktree baseline (natives floor: 312 pass / 378 fail in fresh worktree if natives unbuilt — build natives first or treat as floor).
- **PR/CI handoff:** `gh --repo GratefulDave/oh-my-pi`; provenance trailer not required (these are fork-authored, not cherry-picks) but commit msg ends Co-Authored-By: Claude; PR body ends Generated with Claude Code.
- **Shim-removal handoff:** delete `computeFileHash` from `hashline/src/format.ts` + its export; re-author `hashline.test.ts` (14) + `edit-diff.test.ts` (2) against SnapshotStore API; verify grep = 0.
- **Version/CHANGELOG handoff:** bump 10 package.json in lockstep; CHANGELOG cumulative Tier A+B+C per Keep-a-Changelog.

## 6. Approval Gates

| Gate | Approver | Required evidence |
| ---- | -------- | ----------------- |
| Semver: 15.5.10 vs 15.6.0 | USER → **DECIDED 15.5.10** | User unsure of external downstream. Resolved to 15.5.10: shim was added THIS cycle and never released, so removal breaks nothing externally shipped; fork mirrors upstream 15.x (upstream at 15.5.9) so an independent minor/major jump would break parity semantics. Revisit only if an external consumer of `@oh-my-pi/hashline.computeFileHash` is later found. |
| Shim removal | verifier (opus) | grep `computeFileHash` in `packages/*/src` = 0; all consumer tests green |
| Version bump | self + verify | 10 package.json consistent; build green |

## 7. Verification Plan

- **Unit/TDD:** touched test file per migration PR (executor self-checks, verifier confirms).
- **Integration:** full `bun test packages/coding-agent` + `packages/hashline` on PR7.
- **CI:** GitHub Actions on each PR.
- **Release smoke:** post-bump build of coding-agent dist.

## 8. Risks / Unknowns / Blockers

- **Facts:** helper exists; 3/6 files partial; edit-diff.test.ts extra; 10 pkgs version-synced.
- **Inferences:** read/search/ast-grep migrations are small (residual removal); ast-edit/file-mentions/benchmark are full.
- **Unknowns:** whether `@oh-my-pi/hashline` has external downstream → drives semver. (USER gate.) Exact call shape in ast-edit/file-mentions/benchmark (resolve in-PR; halt+report if a call site applies patches and needs non-trivial wiring).
- **Blockers:** PR7 blocks on 1-6 merged; version bump blocks on PR7 + semver answer.

## 9. Next Action

- **Recommended next lane:** Lane 1 — launch 6 parallel executor (sonnet) subagents in worktrees, one per file, each with the §5 implementation handoff.
- **Pending user input:** semver decision (does NOT block lanes 1-5; needed before lane 6).

## 10. EXECUTION LOG (live)

- Lanes 1-3 DONE: 6 migration PRs merged — #13 ast-edit, #14 ast-grep, #15 read (threaded session through read.ts helpers), #16 search (also dropped invalid `fileHash` metadata on a recordSparse), #17 file-mentions (ephemeral inline — no ToolSession reachable from `generateFileMentionMessages`, sole caller agent-session.ts:4103), #18 benchmark (ephemeral; added `@oh-my-pi/hashline: workspace:*` dep). `git grep computeFileHash` in all src = 0.
- CI fact: `gh run list` historically empty — Actions enabled but never run on this fork. Local per-PR verification is the merge gate (matches prior #5-#12). PRs merged squash + delete-branch.
- Lane 4-5 IN PROGRESS: PR7 (opus executor) — delete shim from hashline/src/format.ts + coding-agent edit-barrel re-export; re-author hashline.test.ts (14 sites) + edit-diff.test.ts (2 sites). KEY: new ring tags are NOT content-addressed → tests must capture the tag returned by recordContiguous, not recompute via computeFileHash.

### Lane 6 spec (version bump → 15.5.10) — replicate scripts/release.ts cmdRelease FILE EDITS ONLY; do NOT run release.ts (it pushes/tags → npm publish). Land as a PR.
1. `sd '"version": "[^"]+"' '"version": "15.5.10"'` on every NON-private `packages/*/package.json` (bumps the 9 synced pkgs incl. hashline 15.5.9→15.5.10; VERIFY benchmark has `"private": true` so its 0.0.1 is skipped — if not private, exclude it manually).
2. Root `package.json`: regex `("@oh-my-pi/[^"]+":\s*)"[^"]+"` → `$1"15.5.10"` (catalog entries, lines ~23-30).
3. Root `Cargo.toml`: `[workspace.package] version` → 15.5.10.
4. Root `CHANGELOG.md`: add `## [15.5.10] - 2026-05-28` under `[Unreleased]` with cumulative Tier A+B+C summary (PRs #5-#18). release.ts only manages `packages/*/CHANGELOG.md`, so root CHANGELOG is manual.
5. Run `bun scripts/check-spoofed-versions.ts` to confirm consistency. Commit + PR (no tag).

### Doc-cleanup follow-up (separate, non-blocking, after PR7): docs/tools/{edit,read,search}.md reference `computeFileHash()` — update to SnapshotStore/recordContiguous, then regenerate docs-index.generated.ts via scripts/generate-docs-index.ts.
