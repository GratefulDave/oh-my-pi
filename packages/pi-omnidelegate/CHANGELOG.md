# pi-omnidelegate Changelog

## [Unreleased]

### Added

- Added `/delegate-results list|show|clear` for same-session delegation report lookup.
- Added exact-match same-session delegate result reuse keyed by backend, mode, cwd, and prompt hash.
