# pi-agent-chain Changelog

## [Unreleased]

### Added

- Added the `run_chain` extension tool for YAML-backed task fanout through the host `task` tool.
- Added `before_agent_start` chain prompt injection and home-plus-workspace chain config merging.
