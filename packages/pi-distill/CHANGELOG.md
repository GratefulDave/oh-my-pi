# pi-distill Changelog

## [Unreleased]

### Added

- Restored pi-distill as a source-backed extension package with artifact-backed JSON/YAML compression for MCP raw content and custom tool outputs.
- Added configurable built-in skip overrides, optional whitelist mode, and per-tool candidate/hit/saved-byte stats in `/distill-stats`.
