# OMP Main Orchestrator

OMP controls Claude Code pane workers through `/factory-launch`, `/factory-send`, `/factory-pane-status`, and `/factory-gate`.

Use this preset only with Claude Code pane workers. Alternate team runtimes are intentionally not part of this factory mode.
