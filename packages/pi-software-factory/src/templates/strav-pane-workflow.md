# Strav Pane Workflow

Strav or cmux may create or observe panes for this run, but `.omp/factory/runs/{{runId}}/` remains the shared source of record for plan state, assignments, gates, and evidence.
