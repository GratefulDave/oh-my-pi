# Factory Lane Assignment

Run: `{{runId}}`
Lane: `{{laneId}}`
Title: {{laneTitle}}
Role: `{{role}}`

## Objective
{{objective}}

## Acceptance
- Complete only the assigned lane work.
- Record completion evidence under `.omp/factory/runs/{{runId}}/evidence/` or summarize it in your final message.
- Use only the assigned Claude Code pane and this run's `.omp/factory/runs/{{runId}}/` artifacts; do not launch alternate team runtimes.
