# pi-software-factory Changelog

## [Unreleased]

### Added

- Added real factory safety-rule evaluation from `.omp/factory/safety.rules.json`.
- Added `/factory-init --dry-run`, `/factory-init --list-presets`, and `/factory-upgrade --dry-run`.

- Added pane-backed factory run planning, cmux Claude Code pane launch/control commands, and gate/run state persistence under `.omp/factory/runs/`.